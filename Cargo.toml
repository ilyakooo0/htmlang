[package]
name = "htmlang"
version = "0.1.0"
edition = "2024"

[dependencies]
notify = "7"
tokio = { version = "1", features = ["io-std", "io-util", "macros", "net", "rt-multi-thread", "sync"] }
tower-lsp = "0.20"
