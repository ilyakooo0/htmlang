@page Variable Filters
@let name hello world
@let greeting hi

@column [spacing 10]
  @text $name|uppercase
  @text $name|lowercase
  @text $name|capitalize
  @text $name|length
  @text $name|reverse
  @text $name|truncate:5
  @text $greeting|default:fallback
